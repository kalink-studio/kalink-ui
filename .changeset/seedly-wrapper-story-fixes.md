---
"@kalink-ui/seedly": patch
"@kalink-ui/seedly-react": patch
---

Use Seedly wrapper components consistently in stories and remove redundant story-level className props that wrappers already provide.
Add `Radio.Group` in `seedly-react` and tune dark-mode contrast for toggle-group, toolbar, and dialog overlays.
